// /components/modals/EditModal/editModal.types.ts

import { Transacao } from '@/app/models/transaction.model'; // Verifique se o caminho está correto

export interface EditModalProps {
  transaction: Transacao;
}